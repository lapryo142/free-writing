self.addEventListener('push',event=>{let d={};try{d=event.data?event.data.json():{}}catch{d={body:event.data?.text()||''}};event.waitUntil(self.registration.showNotification(d.title||'Free Writing',{body:d.body||'A gentle nudge to review your writing.',icon:'/apple-touch-icon.png?v=12.5.8',badge:'/apple-touch-icon.png?v=12.5.8',tag:'freewriting-daily-reminder',renotify:false,data:{url:d.url||'/'}}))});
self.addEventListener('notificationclick',event=>{event.notification.close();const url=event.notification.data?.url||'/';event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{for(const c of list){if('focus'in c){c.navigate(url);return c.focus()}}return clients.openWindow?clients.openWindow(url):undefined}))});
